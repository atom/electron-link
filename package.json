{
  "name": "electron-link",
  "version": "0.0.1",
  "description": "Generates scripts to pass to V8's `mksnapshot` tool.",
  "main": "lib/generate-snapshot-script",
  "scripts": {
    "test": "mocha test/**/*.test.js --ui=tdd",
    "tdd": "electron-mocha test/**/*.test.js --ui=tdd --renderer --interactive"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "dedent": "^0.6.0",
    "electron": "^1.4.15",
    "electron-mocha": "^3.3.0",
    "mocha": "^3.1.2"
  },
  "dependencies": {
    "ast-util": "^0.6.0",
    "recast": "^0.11.17",
    "resolve": "^1.1.7"
  }
}
